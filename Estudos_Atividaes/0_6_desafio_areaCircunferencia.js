// PI * raio * raio

let raio = 10;
const pi = 3.141592;
let circunferenciaArea = pi * raio * raio;

console.log ("O valor da área é " + circunferenciaArea + "m2.")

// Chamando o valor de PI através do Math.
console.log (Math.PI);